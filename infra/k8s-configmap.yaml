apiVersion: v1
kind: ConfigMap
metadata:
  name: pga-config
  namespace: pga-system
data:
  GCP_PROJECT: "your-gcp-project-id"
  GCP_REGION: "us-central1"
  LLM_ENDPOINT: "projects/.../locations/.../models/your-gemini"
